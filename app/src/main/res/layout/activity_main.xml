<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="[http://schemas.android.com/apk/res/android](http://schemas.android.com/apk/res/android)"
    xmlns:tools="[http://schemas.android.com/tools](http://schemas.android.com/tools)"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="#A5D6A7"
    tools:context=".MainActivity">
   <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Parque das Mangabeiras"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Conheça o Parque"
        android:textSize="20sp"
        android:layout_marginBottom="16dp" />

    <!-- Botão para a Curiosidade 1 -->
    <Button
        android:id="@+id/buttonCuriosity1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Curiosidade 1: Curiosidade sobre a flora"
        android:layout_marginBottom="12dp" />

    <!-- Botão para a Curiosidade 2 -->
    <Button
        android:id="@+id/buttonCuriosity2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Curiosidade 2: Atividades de lazer"
        android:layout_marginBottom="12dp" />

    <!-- Botão para a Curiosidade 3 -->
    <Button
        android:id="@+id/buttonCuriosity3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Curiosidade 3: Fauna local"
        android:layout_marginBottom="12dp" />

    <!-- Botão para a Curiosidade 4 -->
    <Button
        android:id="@+id/buttonCuriosity4"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Curiosidade 4: O nome 'Mangabeiras'"
        android:layout_marginBottom="24dp" />

    <!-- Botão para ligar para a prefeitura -->
    <Button
        android:id="@+id/buttonCall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ligar para a Prefeitura"
        android:backgroundTint="#4CAF50"
        android:layout_marginBottom="12dp" />

    <!-- Botão para o site/Instagram -->
    <Button
        android:id="@+id/buttonWebsite"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Visitar Site"
        android:backgroundTint="#2196F3" />

</LinearLayout>
